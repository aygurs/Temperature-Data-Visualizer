#include "Candlestick.h"

Candlestick::Candlestick(std::string date, double open, double close, double high, double low)
:   date(date), 
    open(open), 
    close(close), 
    high(high), 
    low(low) 
{
    //Gets data from initialisation
}